# Cassette for TB8 — STT replay of the bart-test clip, per ADR-0021.
#
# segments is an ordered list of (audio_sha256, transcript) pairs, one per
# Transcribe call. The bart-test clip is a single utterance, so one segment.
# audio_sha256 is the sha256 of the little-endian int16 PCM sample stream the
# recognizer is fed; computed by voicecassette.HashFrames over the 32 ms-framed
# bart-test clip (16 kHz, mono, 16-bit). A mismatch fails the test and points
# at -tags=record.
#
# transcript is recorded from the live ElevenLabs scribe_v2 adapter under
# -tags=record (ADR-0021); re-run that to refresh it.
segments:
    - audio_sha256: c49e6cc75851d37e8a1749ee6e420205f3839069c068c60d2dadb824106ae283
      transcript: Bart, what's the special tonight?
notes: |-
    Plumbing bring-up cassette for TB8 (ADR-0021). The clip's audio was
    generated via ElevenLabs Mark/eleven_v3 to give the address detector a
    "Bart" mention to match.

    Re-recorded against ElevenLabs scribe_v2 on 2026-05-21.

    Re-recorded against ElevenLabs scribe_v2 on 2026-05-27.

    Re-recorded against ElevenLabs scribe_v2 on 2026-06-18.
